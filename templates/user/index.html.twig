{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>/home/kilari/php/ah/php-eval-skel-2025-5/src/Controller/UserController.php</code></li>
        <li>Your template at <code>/home/kilari/php/ah/php-eval-skel-2025-5/templates/user/index.html.twig</code></li>
    </ul>
</div>
    {% extends 'base.html.twig' %}

    {% block body %}
        <div class="container">
            <h1>Who to Follow</h1>

            <div class="user-list">
                {% for user in users %}
                    {# Don't show yourself in the list #}
                    {% if app.user and user != app.user %}
                        <div class="card mb-3">
                            <div class="card-body">
                                <h5 class="card-title">{{ user.email }}</h5> <a href="{{ path('app_message_send', {id: user.id}) }}" class="btn btn-primary">
                                    Message
                                </a>

                                <a href="#" class="btn btn-outline-secondary">Follow</a>
                            </div>
                        </div>
                    {% endif %}
                {% else %}
                    <p>No other users found.</p>
                {% endfor %}
            </div>
        </div>
    {% endblock %}
{% endblock %}
