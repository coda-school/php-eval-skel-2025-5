{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Send a DM to {{ recipient.email }}</h1> {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Send Message</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
